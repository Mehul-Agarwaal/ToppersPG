<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PG Admin Dashboard</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Inter', sans-serif;
            background-color: #f8fafc;
        }
    </style>
</head>
<body class="antialiased">
    <header class="bg-white shadow-md">
        <nav class="container mx-auto px-4 md:px-6 py-4 flex justify-between items-center">
            <div class="text-2xl font-bold text-indigo-600">
                PG Admin
            </div>
            <div class="flex items-center space-x-4">
                <% if (user) { %>
                    <span class="text-sm text-gray-700 hidden sm:block">Welcome, <%= user.username %></span>
                    <a href="/" class="px-3 py-2 rounded-md text-sm font-medium <%= page === 'dashboard' ? 'bg-indigo-100 text-indigo-700' : 'text-gray-700 hover:bg-gray-100' %>">Dashboard</a>
                    <a href="/residents" class="px-3 py-2 rounded-md text-sm font-medium <%= page === 'residents' || page === 'add-resident' ? 'bg-indigo-100 text-indigo-700' : 'text-gray-700 hover:bg-gray-100' %>">Residents</a>
                    <a href="/auth/logout" class="px-3 py-2 rounded-md text-sm font-medium text-red-600 hover:bg-red-50">Logout</a>
                <% } %>
            </div>
        </nav>
    </header>